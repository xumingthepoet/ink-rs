[package]
name = "ink-experiments"
version = "0.1.0"
edition.workspace = true
description = "Executable experiments for the current ink-rs language surface."
publish = false

[lib]
doctest = false
test = false

[dependencies]
ink-compiler = { path = "../ink-compiler" }
ink-runtime = { path = "../ink-runtime" }
serde_json = "1"
