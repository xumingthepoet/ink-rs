[package]
name = "ink-compiler"
version = "0.1.0"
edition.workspace = true

[dependencies]
ink-story-json-format = { version = "0.1.0", path = "../ink-story-json-format" }

[dev-dependencies]
serde_json = "1"
