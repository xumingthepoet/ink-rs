[package]
name = "ink-test"
version = "0.1.0"
edition.workspace = true
publish = false

[dependencies]
ink_runtime = { package = "ink-runtime", path = "../ink-runtime" }
ink_compiler = { package = "ink-compiler", path = "../ink-compiler" }
serde_json = "1"
