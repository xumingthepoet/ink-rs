Story
  Weave(baseIndent=0)
  Module(name="game")
    Flow(level=Knot, name="main", function=false)
      Weave(baseIndent=0)
        Divert(target="-> A", empty=false, tunnel=true)
        Text("We will never return to here!")
        Text("\n")
    Flow(level=Knot, name="A", function=false)
      Weave(baseIndent=0)
        Text("This is A")
        Text("\n")
        TunnelOnwards
    Flow(level=Knot, name="B", function=false)
      Weave(baseIndent=0)
        Text("Now in B.")
        Text("\n")
