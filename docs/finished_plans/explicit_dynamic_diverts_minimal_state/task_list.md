# Task List: Explicit Dynamic Diverts and Minimal Stateful Runtime

Progress: 60/60

Status key: `[ ]` pending, `[~]` in progress, `[x]` complete, `[!]` blocked.

Update rule: when a task is completed, change its status to `[x]` and increment the progress counter. Do not mark a task complete until its acceptance condition is true. Keep this file synchronized during implementation.

| ID | Status | Goal | Method | Acceptance | Forbidden | Modification Boundary |
| --- | --- | --- | --- | --- | --- | --- |
| 001 | [x] | Establish the starting point. | Read this task list, the plan file, `AGENTS.md`, and `Notes.md`; record any discovered blockers in this file. | The implementer can state the active scope without guessing. | Do not edit code during this task. | Documentation reads only. |
| 002 | [x] | Capture baseline repository state. | Run `git status --short` and inspect existing uncommitted changes. | Any unrelated dirty work is identified before edits start. | Do not revert or stage unrelated user changes. | Git inspection only. |
| 003 | [x] | Capture baseline validation signals. | Run the smallest relevant existing tests for parser, language, runtime save/load, and choice behavior. | Current failures are known before implementation. | Do not update fixtures to hide baseline failures. | Test commands and generated build artifacts only. |
| 004 | [x] | Inventory count and turn dependencies. | Search compiler, runtime, tests, and docs for `READ_COUNT`, `TURNS`, `TURNS_SINCE`, read-count shorthand, visit counts, turn indices, and `turnIdx`. | All count and turn touchpoints are listed or understood. | Do not delete code yet. | Read-only search. |
| 005 | [x] | Inventory choice behavior dependencies. | Search parser, parsed model, lowering, runtime, tests, and docs for once-only flags, `choice_only_content`, bracket choice syntax, and choice save JSON. | The implementation surface for new choice semantics is known. | Do not change choice behavior yet. | Read-only search. |
| 006 | [x] | Inventory dynamic divert dependencies. | Search parser, parsed model, analysis, lowering, fixtures, and docs for `DivertTarget`, variable divert, tunnel onwards, and dynamic target lowering. | The implementation surface for `-> {expr}` is known. | Do not add parser branches yet. | Read-only search. |
| 007 | [x] | Inventory multi-flow dependencies. | Search runtime public APIs, state, tests, and docs for `switch_flow`, `remove_flow`, `named_flows`, and `currentFlowName`. | All multi-flow removal points are known. | Do not remove public API before tests are planned. | Read-only search. |
| 008 | [x] | Inventory save-state JSON dependencies. | Inspect `StoryState`, `Flow`, `CallStack`, choice JSON, and variable-state serialization. | The v1 save fields and their runtime uses are documented locally. | Do not mutate save JSON before schema tests exist. | Read-only search. |
| 009 | [x] | Add parser tests for braced dynamic diverts. | Add focused tests for `-> {next}`, `-> {route.next}`, `-> {targets[0]}`, `-> {pick(flag)}`, and `-> {next}(value)`. | Tests fail before parser implementation and pass after it. | Do not weaken existing static divert tests. | Compiler parser tests only. |
| 010 | [x] | Add parser tests for dynamic tunnels. | Add focused tests for `-> {next} ->` and tunnel-onwards dynamic overrides if supported by the plan. | Dynamic tunnel syntax has explicit parse coverage. | Do not silently reinterpret static tunnel syntax. | Compiler parser tests only. |
| 011 | [x] | Add parser tests for removed choice brackets. | Add tests that `[...]` in choice text produces a clear diagnostic. | Invalid bracket choice syntax is pinned. | Do not treat brackets as ordinary text unless the plan changes. | Compiler parser tests only. |
| 012 | [x] | Add analysis tests for old variable diverts. | Add tests rejecting `-> next` when `next` is a global, temp, const, or parameter. | Diagnostics point users to `-> {next}`. | Do not reject static paths that are not visible variables. | Compiler analysis tests only. |
| 013 | [x] | Add analysis tests for dynamic target type checking. | Add tests accepting `-> {target_value}` and rejecting `-> {int_value}` or non-target field expressions. | Dynamic target expressions must have type `->`. | Do not rely on runtime errors for type mistakes. | Compiler analysis tests only. |
| 014 | [x] | Add regression tests for dotted field collisions. | Add a story where `player.hp` is both a struct field access and a static label path. | `{player.hp}` reads the field, not a read-count path. | Do not solve this with fixture-specific paths. | Compiler/language tests only. |
| 015 | [x] | Add runtime tests for generated choice save. | Add a save/load test at a choice pause point with generated choices and thread snapshots. | Loading the save allows choosing without regenerating choices. | Do not assume choices are pure or side-effect-free. | Runtime tests only. |
| 016 | [x] | Add runtime tests for repeatable choices. | Add tests showing both `*` and `+` choices remain available after revisiting. | `*` and `+` behave equivalently for repeatability. | Do not preserve once-only hiding. | Runtime or language fixtures only. |
| 017 | [x] | Add runtime tests for no selected-text echo. | Add a story where choice-line text differs from branch content. | Selecting a choice does not print the choice display text unless authored in branch content. | Do not remove branch content output. | Runtime or language fixtures only. |
| 018 | [x] | Add runtime tests for choice conditions. | Add tests where `{condition}` hides and shows repeatable choices. | Conditions still control visibility. | Do not remove conditional choice support. | Runtime or language fixtures only. |
| 019 | [x] | Add save tests for deterministic random. | Add a save/load test around `RANDOM` and `SEED_RANDOM`. | Random sequence continues deterministically after load. | Do not remove `storySeed` or `previousRandom`. | Runtime save/load tests only. |
| 020 | [x] | Add tests for removed count and turn features. | Add compile-time or API tests for rejected count/turn builtins and APIs. | Removed features fail clearly. | Do not leave old behavior as hidden compatibility. | Compiler and runtime public API tests only. |
| 021 | [x] | Extend parsed divert model for dynamic targets. | Add a parsed representation for braced dynamic target expressions. | Static and dynamic direct diverts are distinguishable without string guessing. | Do not encode dynamic targets as raw path strings. | `crates/ink-compiler/src/parsed` only. |
| 022 | [x] | Parse braced dynamic direct diverts. | Update divert parsing to recognize `-> {expr}` as a dynamic target. | Parser tests for simple dynamic diverts pass. | Do not affect `Expression::DivertTarget` value literals. | Divert syntax parser only. |
| 023 | [x] | Parse dynamic divert arguments. | Support `-> {expr}(args...)` with the existing argument parsing rules. | Dynamic target arguments parse in order and reject malformed calls. | Do not treat `{expr}(args)` as a static target. | Divert syntax parser only. |
| 024 | [x] | Parse dynamic tunnel forms. | Support dynamic targets in tunnel and tunnel-onwards positions according to tests. | Dynamic tunnel parser tests pass. | Do not break static `-> target ->` or `->-> target` unless explicitly removed. | Divert syntax parser only. |
| 025 | [x] | Produce migration diagnostics for old variable diverts. | In target analysis, detect bare targets rooted in visible variables or constants and report `-> {name}` guidance. | Old variable divert tests fail with clear diagnostics. | Do not fall back to static target resolution after detecting a visible variable root. | Target analysis only. |
| 026 | [x] | Type-check dynamic target expressions. | Use existing expression inference to require type `->` for braced dynamic diverts. | Non-target dynamic expressions are rejected at compile time. | Do not rely on lowering or runtime validation. | Analysis expression and target checks only. |
| 027 | [x] | Keep static divert resolution static-only. | Resolve bare `-> knot.stitch` only through static story target lookup. | Static targets still compile and dynamic variables require braces. | Do not inspect runtime variable scopes for static diverts except to produce migration diagnostics. | Target analysis only. |
| 028 | [x] | Lower braced dynamic diverts. | Evaluate the expression into the existing dynamic divert temp and emit a variable divert. | Dynamic divert runtime fixtures pass. | Do not duplicate analysis type inference in ad hoc lowering guesses. | Compiler lowering only. |
| 029 | [x] | Lower dynamic divert arguments correctly. | Preserve current argument evaluation order for `-> {target}(args...)`. | Parameterized dynamic divert tests pass. | Do not change static divert argument behavior unintentionally. | Compiler lowering only. |
| 030 | [x] | Lower dynamic tunnel targets correctly. | Apply the same dynamic target lowering to retained tunnel forms. | Dynamic tunnel tests pass. | Do not remove thread or tunnel support. | Compiler lowering only. |
| 031 | [x] | Fix field/index expression lowering precedence. | Ensure roots that are visible variables or constants lower as data access, not count lookup. | `player.hp` collision regression passes. | Do not reintroduce path-string guessing for variable-rooted expressions. | Expression lowering only. |
| 032 | [x] | Preserve constant expansion in dynamic expressions. | Expand constants inside dynamic target expressions, including struct fields and arrays. | `CONST fallback: ->` and `CONST route.next` dynamic divert tests pass. | Do not turn constants into runtime variables. | Expression/value lowering only. |
| 033 | [x] | Remove read-count shorthand from expressions. | Remove or reject `{knot}` as implicit read count. | Count-shorthand rejection tests pass. | Do not reinterpret `{knot}` as a variable unless it is a real variable. | Expression analysis and lowering only. |
| 034 | [x] | Remove count and turn builtins from analysis. | Remove `READ_COUNT`, `TURNS`, `TURNS_SINCE`, and `CHOICE_COUNT` from builtin typing. | Removed builtin tests fail with clear diagnostics. | Do not leave partially supported lowering paths. | Compiler analysis only. |
| 035 | [x] | Remove count and turn builtin lowering. | Delete or disable lowering for removed builtins and related metadata collection. | Compiler no longer emits count/turn runtime commands for source builtins. | Do not remove retained `RANDOM` or `SEED_RANDOM`. | Compiler lowering only. |
| 036 | [x] | Remove visit-count runtime state. | Remove save/runtime mutation paths for visit counts that are no longer needed. | Runtime compiles without public visit-count behavior. | Do not remove container navigation itself. | Runtime state and navigation only. |
| 037 | [x] | Remove turn-count runtime state. | Remove turn indices and `turnIdx` from state and save JSON while retaining RNG state. | Save JSON no longer includes turn fields. | Do not remove `storySeed` or `previousRandom`. | Runtime state and save JSON only. |
| 038 | [x] | Remove count and turn public APIs. | Remove or deprecate public methods tied only to visit and turn counts. | API tests and documentation no longer expose removed APIs. | Do not remove unrelated variable get/set APIs. | Runtime public API only. |
| 039 | [x] | Make `*` and `+` equivalent in parsing/model. | Keep both bullets accepted but stop treating `*` as once-only. | Parsed snapshots and runtime behavior no longer depend on once-only. | Do not remove choice nesting depth behavior. | Choice parser and parsed model only. |
| 040 | [x] | Remove choice-only bracket semantics. | Reject `[...]` during choice parsing with a migration diagnostic. | Bracket rejection tests pass. | Do not silently preserve old bracket semantics. | Choice parser only. |
| 041 | [x] | Lower choice display text only. | Adjust lowering so choice-line text becomes display text and is not branch output by default. | No selected-text echo tests pass. | Do not suppress explicitly authored branch content. | Choice lowering only. |
| 042 | [x] | Remove once-only runtime hiding. | Stop checking visit count when generating choices. | Repeatable choice tests pass for `*` and `+`. | Do not remove conditional choice checks. | Runtime choice generation only. |
| 043 | [x] | Preserve choice condition runtime behavior. | Keep condition evaluation and truthiness checks for choices. | Conditional choice tests pass. | Do not delete condition flags or condition lowering. | Choice lowering/runtime only. |
| 044 | [x] | Design v2 save JSON shape in code. | Introduce v2 constants and structs/helpers for minimal state fields. | New save fields are centralized and testable. | Do not emit old v1 save fields. | Runtime save-state code only. |
| 045 | [x] | Serialize thread stacks and temps. | Save active thread callstacks with frame path, index, push/pop type, expression flag if still required, and temps. | Threaded choice save/load tests can restore the choosing context. | Do not serialize output stream or eval stack. | Callstack and save JSON only. |
| 046 | [x] | Serialize globals. | Keep variable-state serialization for all globals with defaults handled correctly. | Existing variable save/load tests still pass or are intentionally updated. | Do not serialize temps as globals. | Variables state save/load only. |
| 047 | [x] | Serialize generated choices. | Save current choice text, target path, tags, source path if needed, and choice thread snapshot. | Loading at a choice point exposes the same choices without regeneration. | Do not rerun choice generation during load. | Choice JSON and save-state JSON only. |
| 048 | [x] | Serialize deterministic RNG state. | Keep `storySeed` and `previousRandom` in v2 save. | Random save/load tests pass. | Do not reintroduce turn index fields. | Runtime save-state JSON only. |
| 049 | [x] | Reject unsupported mid-step saves. | Add checks that prevent saving while expression evaluation, output buffering, or divert target internals are active. | Invalid save timing returns a clear error. | Do not serialize `evalStack`, `outputStream`, or `currentDivertTarget`. | Runtime save-state API only. |
| 050 | [x] | Load v2 save state. | Reconstruct globals, RNG, thread stacks, current pointer, and generated choices from v2 JSON. | New save/load tests pass. | Do not accept old v1 saves. | Runtime load-state code only. |
| 051 | [x] | Reject old save JSON. | Bump save version and keep old save loading as a version mismatch. | Old save version test reports the expected error. | Do not implement lossy migration unless the plan changes. | Runtime save-state versioning only. |
| 052 | [x] | Remove multi-flow public API. | Remove `switch_flow`, `remove_flow`, current-flow APIs, and tests that only cover multi-flow. | Runtime API no longer exposes multi-flow. | Do not remove threads or normal callstack support. | Runtime public API and tests only. |
| 053 | [x] | Remove named-flow save state. | Delete `named_flows`, `flows`, and `currentFlowName` save/load paths. | Save JSON contains no multi-flow map. | Do not remove the default execution flow state needed by normal stories. | Runtime state/save only. |
| 054 | [x] | Update compiler fixtures. | Update fixtures for explicit dynamic diverts, removed counts/turns, repeatable choices, and removed brackets. | Compiler fixture snapshots reflect intentional language changes. | Do not edit expected output merely to hide failures. | `crates/ink-test/fixtures` only. |
| 055 | [x] | Update C# compatibility tests intentionally. | Remove, rewrite, or mark obsolete tests whose upstream behavior was intentionally removed. | C# compatibility suite reflects documented divergence. | Do not skip unrelated failing tests. | `crates/ink-test/tests/csharp_tests` only. |
| 056 | [x] | Update WritingWithInk docs. | Update `WritingWithInk-updates.md` first, then apply changes to `WritingWithInk-latest.md`. | Docs describe new divert, choice, count, turn, save, and multi-flow semantics. | Do not edit `docs/WritingWithInk-origin.md`. | Maintained docs only. |
| 057 | [x] | Update runtime format and architecture docs. | Update save-state/runtime-state sections where they mention old fields or APIs. | Docs no longer describe removed save fields as current behavior. | Do not change compiled story JSON docs unless the text is directly misleading. | `docs/Architecture.md` and runtime docs only. |
| 058 | [x] | Update editor grammar and README if needed. | Add highlighting for `-> {expr}` and remove guidance for old variable diverts or choice brackets. | Editor fixtures or manual inspection show syntax is recognized. | Do not overwrite unrelated dirty highlighter changes. | `editor/vscode-ink-rs` only. |
| 059 | [x] | Run focused validation. | Run parser, compiler language, runtime save/load, and choice-focused tests. | Focused suites pass or failures are tied to remaining tasks. | Do not proceed to broad fixture rewrites while focused tests are unexplained. | Test commands only. |
| 060 | [x] | Run final validation and close plan. | Run `cargo fmt --all --check`, `cargo test --workspace`, and `make gate`; move completed plan to `docs/finished_plans/` only after success. | Full gate passes and the active plan is no longer in `docs/active_plan/`. | Do not move the plan before implementation and validation are complete. | Validation and docs plan-directory move only. |
